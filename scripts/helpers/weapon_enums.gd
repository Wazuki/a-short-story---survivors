class_name  WeaponEnums
extends RefCounted

# Handles all the enumerations for weapons.
enum SceneKey { BULLET, SECONDARY_BULLET, IMPACT_EFFECT, TRAIL_EFFECT }
enum Type { SLAM, LIGHT_BLADE, WALDOS, ARROW, CHAIN_LIGHTNING}
enum TargetType { NONE, CLOSEST, HIGHEST_HP, RANDOM }