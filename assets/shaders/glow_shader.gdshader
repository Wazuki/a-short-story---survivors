shader_type canvas_item;

void fragment() {
    // vec4 tex_color = texture(TEXTURE, UV);
    // COLOR = tex_color * vec4(1.0, 1.0, 1.5, 1.0); // Extra blue glow
    // COLOR.rgb *= smoothstep(0.2, 0.8, tex_color.a); // Soft glow edges
}